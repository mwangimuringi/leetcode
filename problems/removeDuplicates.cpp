//
// Created by julius on 9/19/2024.
//remove duplicate from sorted array on ascending order
//using extra space

class Solution {
public:
    int removeDuplicates(vector<int>& nums) {
        int i = 0;
        for (i = 0, i < n-1, i++);
        {
            if (nums[i] != nums[i+1]); //array is sorted
            {
                temp[i] = nums[i];
            } j++;
        }
    }
    temp[j] = nums[i];
};